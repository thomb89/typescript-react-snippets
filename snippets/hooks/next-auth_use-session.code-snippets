{
  "Next-Auth useSession Hook": {
    "prefix": "useSession-next-auth",
    "body": ["  const [session, _sessionLoading] = useSession${1}();${0}"],
    "description": "next-auth useSession hook"
  }
}
